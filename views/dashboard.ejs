<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Laboratorio Glorimar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .navbar-brand {
            font-weight: bold;
            color: #28a745 !important;
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            transition: transform 0.2s;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }

        .experiments-icon {
            color: #28a745;
        }

        .reactivos-icon {
            color: #17a2b8;
        }

        .solicitudes-icon {
            color: #ffc107;
        }

        .users-icon {
            color: #dc3545;
        }

        .welcome-card {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border-radius: 15px;
        }

        .user-info {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 15px;
            margin-top: 20px;
        }

        .btn-logout {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
        }

        .btn-logout:hover {
            background: rgba(255, 255, 255, 0.3);
            color: white;
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">
                <i class="fas fa-flask me-2"></i>Laboratorio Glorimar
            </a>

            <div class="navbar-nav ms-auto">
                <span class="navbar-text me-3">
                    <i class="fas fa-user me-1"></i>
                    <%= user.username %> (<%= user.role %>)
                </span>
                <form action="/auth/logout-web" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-outline-light btn-sm">
                        <i class="fas fa-sign-out-alt me-1"></i>Cerrar Sesión
                    </button>
                </form>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <!-- Mensajes de alerta -->
        <% if (typeof error !=='undefined' && error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-triangle me-2"></i>
                <%= error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            <% } %>

                <% if (typeof success !=='undefined' && success) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="fas fa-check-circle me-2"></i>
                        <%= success %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                    <% } %>

                        <!-- Tarjeta de bienvenida -->
                        <div class="row mb-4">
                            <div class="col-12">
                                <div class="card welcome-card">
                                    <div class="card-body text-center py-5">
                                        <h2 class="card-title">
                                            <i class="fas fa-tachometer-alt me-2"></i>
                                            ¡Bienvenido al Sistema de Gestión!
                                        </h2>
                                        <p class="card-text lead mb-4">
                                            Administra experimentos, reactivos, solicitudes y usuarios desde un solo
                                            lugar
                                        </p>

                                        <div class="user-info">
                                            <h5><i class="fas fa-user-circle me-2"></i>Información de Usuario</h5>
                                            <p class="mb-1"><strong>Usuario:</strong>
                                                <%= user.username %>
                                            </p>
                                            <p class="mb-1"><strong>Email:</strong>
                                                <%= user.email %>
                                            </p>
                                            <p class="mb-0"><strong>Rol:</strong>
                                                <span class="badge bg-light text-dark">
                                                    <%= user.role %>
                                                </span>
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Módulos del sistema -->
                        <div class="row">
                            <!-- Experimentos -->
                            <div class="col-md-6 col-lg-3 mb-4">
                                <div class="card h-100">
                                    <div class="card-body text-center">
                                        <div class="experiments-icon">
                                            <i class="fas fa-flask card-icon"></i>
                                        </div>
                                        <h5 class="card-title">Experimentos</h5>
                                        <p class="card-text text-muted">Gestiona el catálogo de experimentos</p>
                                        <a href="/examenes" class="btn btn-success">
                                            <i class="fas fa-eye me-1"></i>Ver Experimentos
                                        </a>
                                        <% if (user.role==='admin' || user.role==='user' ) { %>
                                            <a href="/examenes/crear" class="btn btn-outline-success ms-1">
                                                <i class="fas fa-plus me-1"></i>Agregar
                                            </a>
                                            <% } %>
                                    </div>
                                </div>
                            </div>

                            <!-- Reactivos -->
                            <div class="col-md-6 col-lg-3 mb-4">
                                <div class="card h-100">
                                    <div class="card-body text-center">
                                        <div class="reactivos-icon">
                                            <i class="fas fa-vial card-icon"></i>
                                        </div>
                                        <h5 class="card-title">Reactivos</h5>
                                        <p class="card-text text-muted">Administra el inventario de reactivos</p>
                                        <a href="/insumos" class="btn btn-info text-white">
                                            <i class="fas fa-eye me-1"></i>Ver Reactivos
                                        </a>
                                        <% if (user.role==='admin' || user.role==='user' ) { %>
                                            <a href="/insumos/crear" class="btn btn-outline-info ms-1">
                                                <i class="fas fa-plus me-1"></i>Agregar
                                            </a>
                                            <% } %>
                                    </div>
                                </div>
                            </div>

                            <!-- Solicitudes -->
                            <div class="col-md-6 col-lg-3 mb-4">
                                <div class="card h-100">
                                    <div class="card-body text-center">
                                        <div class="solicitudes-icon">
                                            <i class="fas fa-clipboard-list card-icon"></i>
                                        </div>
                                        <h5 class="card-title">Solicitudes</h5>
                                        <p class="card-text text-muted">Gestiona las solicitudes de material</p>
                                        <% if (user.role==='admin' || user.role==='user' ) { %>
                                            <a href="/ordenes" class="btn btn-warning">
                                                <i class="fas fa-eye me-1"></i>Ver Solicitudes
                                            </a>
                                            <a href="/ordenes/crear" class="btn btn-outline-warning ms-1">
                                                <i class="fas fa-plus me-1"></i>Crear
                                            </a>
                                            <% } else { %>
                                                <span class="text-muted small">Solo lectura</span>
                                                <% } %>
                                    </div>
                                </div>
                            </div>

                            <!-- Usuarios (solo admin) -->
                            <% if (user.role==='admin' ) { %>
                                <div class="col-md-6 col-lg-3 mb-4">
                                    <div class="card h-100">
                                        <div class="card-body text-center">
                                            <div class="users-icon">
                                                <i class="fas fa-users card-icon"></i>
                                            </div>
                                            <h5 class="card-title">Usuarios</h5>
                                            <p class="card-text text-muted">Administra usuarios del sistema</p>
                                            <a href="/admin/usuarios" class="btn btn-danger">
                                                <i class="fas fa-cog me-1"></i>Gestionar
                                            </a>
                                        </div>
                                    </div>
                                </div>
                                <% } %>
                        </div>

                        <!-- Información del sistema -->
                        <div class="row mt-4">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-body">
                                        <h5 class="card-title">
                                            <i class="fas fa-info-circle me-2"></i>Información del Sistema
                                        </h5>
                                        <div class="row">
                                            <div class="col-md-4">
                                                <p class="mb-1"><strong>Versión:</strong> 3.0.0</p>
                                                <p class="mb-1"><strong>Estado:</strong>
                                                    <span class="badge bg-success">Operativo</span>
                                                </p>
                                            </div>
                                            <div class="col-md-4">
                                                <p class="mb-1"><strong>Autenticación:</strong> JWT + Sesiones</p>
                                                <p class="mb-1"><strong>Base de datos:</strong> MySQL/MariaDB</p>
                                            </div>
                                            <div class="col-md-4">
                                                <p class="mb-1"><strong>Último acceso:</strong>
                                                    <%= new Date().toLocaleString('es-ES') %>
                                                </p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>