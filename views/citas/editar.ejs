<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 30px;
            margin-bottom: 30px;
            max-width: 600px;
        }

        .btn-primary {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            border-radius: 25px;
            padding: 12px 30px;
        }

        .btn-primary:hover {
            background: linear-gradient(45deg, #5a6fd8, #6a4190);
        }

        .form-control,
        .form-select {
            border-radius: 10px;
            border: 2px solid #e9ecef;
            padding: 12px 15px;
        }

        .form-control:focus,
        .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .alert {
            border-radius: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="text-center mb-4">
            <h1 class="mb-2"><i class="fas fa-edit text-primary me-2"></i>
                <%= title %>
            </h1>
            <p class="text-muted">Modifica los datos de la función</p>
        </div>

        <div class="card">
            <div class="card-body">
                <% if (typeof error !=='undefined' && error) { %>
                    <div class="alert alert-danger" role="alert">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <%= error %>
                    </div>
                    <% } %>

                        <form action="/citas/<%= prueba.id %>/editar" method="POST">
                            <div class="mb-3">
                                <label for="id_experimento" class="form-label">
                                    <i class="fas fa-flask me-1"></i>Experimento *
                                </label>
                                <select class="form-select" id="id_experimento" name="id_experimento" required>
                                    <option value="">Seleccione un experimento</option>
                                    <% experimentos.forEach(function(experimento) { %>
                                        <option value="<%= experimento.id %>" <%=prueba.id_experimento==experimento.id
                                            ? 'selected' : '' %>>
                                            <%= experimento.nombre %>
                                        </option>
                                        <% }); %>
                                </select>
                                <div class="form-text">Seleccione el experimento a realizar</div>
                            </div>

                            <div class="mb-3">
                                <label for="id_laboratorio" class="form-label">
                                    <i class="fas fa-building me-1"></i>Laboratorio *
                                </label>
                                <select class="form-select" id="id_laboratorio" name="id_laboratorio" required>
                                    <option value="">Seleccione un laboratorio</option>
                                    <% laboratorios.forEach(function(laboratorio) { %>
                                        <option value="<%= laboratorio.id_laboratorio %>"
                                            <%=prueba.id_laboratorio==laboratorio.id_laboratorio ? 'selected' : '' %>>
                                            <%= laboratorio.nombre %> (<%= laboratorio.ubicacion %>)
                                        </option>
                                        <% }); %>
                                </select>
                                <div class="form-text">Seleccione el laboratorio asignado</div>
                            </div>

                            <div class="mb-4">
                                <label for="fecha_hora_inicio" class="form-label">
                                    <i class="fas fa-clock me-1"></i>Fecha y Hora *
                                </label>
                                <input type="datetime-local" class="form-control" id="fecha_hora_inicio"
                                    name="fecha_hora_inicio"
                                    value="<%= new Date(prueba.fecha_hora_inicio).toISOString().slice(0, 16) %>"
                                    min="<%= new Date().toISOString().slice(0, 16) %>" required>
                                <div class="form-text">Seleccione la fecha y hora de la prueba</div>
                            </div>

                            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                                <a href="/citas" class="btn btn-outline-secondary me-md-2">
                                    <i class="fas fa-times me-1"></i>Cancelar
                                </a>
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-save me-1"></i>Actualizar Prueba
                                </button>
                            </div>
                        </form>
            </div>
        </div>

        <div class="text-center mt-3">
            <small class="text-muted">* Campos obligatorios</small>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-focus on first select
        document.addEventListener('DOMContentLoaded', function () {
            document.getElementById('id_pelicula').focus();
        });
    </script>
</body>

</html>